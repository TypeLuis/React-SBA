import React from 'react';
import type { HeroObj } from '../types/types';

type HeroDetailProps = {
  hero: HeroObj
}

const HeroDetail = ({ hero }: HeroDetailProps) => {
  return (
    <></>
  );
};

export default HeroDetail;