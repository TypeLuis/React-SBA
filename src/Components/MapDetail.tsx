import React from 'react';
import type { RivalsMap } from '../types/types';

type MapDetailProps = {
  rivalsMap : RivalsMap
}

const MapDetail = ({ rivalsMap }: MapDetailProps) => {
  return (
    <></>
  );
};

export default MapDetail;